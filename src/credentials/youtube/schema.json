{
  "$id": "https://example.com/schemas/youtube-account.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "name": "YouTubeAccount",
  "description": "YouTube Account Credential",
  "type": "object",
  "properties": {
    "credentialSubject": {
      "type": "object",
      "properties": {
        "handle": {
          "type": "string"
        }
      },
      "required": ["handle"]
    },
    "issued": {
      "type": "string",
      "format": "date-time"
    }
  },
  "required": ["handle", "issued"]
}
